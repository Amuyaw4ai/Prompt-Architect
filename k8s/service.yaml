apiVersion: v1
kind: Service
metadata:
  name: prompt-architect-service
  labels:
    app: prompt-architect
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: prompt-architect
